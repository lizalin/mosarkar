<?php

    /* ================================================
      File Name      	: QueryAuthentication.php
      Description	: This is used to view Query Authentication.
      Created On    	: 27 Sept 2017
      Created By    	: Manali Ranjan

      Update History 	:	<Updated by>		<Updated On>		<Remarks>

      ================================================== */

    // Set Default Values
    include_once('controller/clsQueryAuthentication.php');
    $objQuery                     = new clsQueryAuthentication;
//    $fromDate                     = (isset($_REQUEST['selFromDate']) && $_REQUEST['selFromDate']>0)?date('Y-m-d',strtotime($_REQUEST['selFromDate'])):date('Y-m-d');
//    $toDate                       = (isset($_REQUEST['selToDate']) && $_REQUEST['selToDate']>0)?date('Y-m-d',strtotime($_REQUEST['selToDate'])):date('Y-m-d');
     //$mobilenm                      = (isset($_REQUEST['txtmobilenm']) && $_REQUEST['txtmobilenm']!=0) ? $_REQUEST['txtmobilenm'] : 0;
//print_r($mobilenm);
     //$result   = $objQuery->InsertQuery('A',0,$mobilenm,'','','','','','');
?>
